// // Built-in functions:

// alert("hi")

// Userdefined functions:

// DRY -> Don't repeat your code

function foo(a, b) {

  let temp = ((a + b) * a) / (a - b)

  return temp;
}

console.log(foo(10, 5));
